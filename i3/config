# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

#exec xrandr --output DP-1-1-2 --above eDP-1-1
set $mod Mod4
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 14
set $monitor2=$(xrandr -q | grep -w connected | awk 'NR==2 {print $1}')
set $monitor1=$(xrandr -q | grep -w connected | awk 'NR==1 {print $1}')
#set $monitor2 DP-1-2
#set $monitor1 eDP-1-1-1
set $volume_index=$(pacmd list-sinks | grep -i index | grep \* |cut -d' ' -f 5)
exec --no-startup-id pacmd set-defult-sink $volume_index
exec --no-startup-id nm-applet 
exec --no-startup-id shadowsocks
exec --no-startup-id netease
exec --no-startup-id google-chrome
#exec --no-startup-id gnome-calculator
exec --no-startup-id nautilus
exec --no-startup-id blueman-applet
exec --no-startup-id urxvt -name dropdown
exec --no-startup-id shutter
exec --no-startup-id zeal
exec --no-startup-id /home/zj/software/libinput-gestures/libinput-gestures
exec killall -q notify-osd
#exec  xrandr --output eDP-1-1 --scale 1x1 --pos 0x2160 --output HDMI-1 --panning 3840x2160+0+0 --scale 2x2 --pos 0x0
#exec xrandr --output $monitor2 --above eDP-1
#exec xrandr --output eDP-1 --auto --scale 1x1 --pos 0x0 --output $monitor2 --panning 3840x2160+3840+0 --auto --scale 2x2 --pos 3840x0
#exec xrandr --output $monitor2 --fb 7680x2160 --mode 1920x1080 --scale 2x2 --pos 3840x0 --output eDP-1 --scale 1x1 --pos 0x0
#exec xrandr  --output eDP-1 --scale 1x1 --pos 0x0
#bindsym $mod+space exec --no-startup-id /home/zj/software/bin/zzzfoo -e="-mime:message/rfc822" -o xdg-open
#exec xrandr --output `xrandr -q | grep -w connected | awk 'NR==2 {print $1}'` --fb 7680x2160 --mode 1920x1080 --scale 2x2 --pos 0x0 --output eDP-1 --scale 1x1 --pos 3840x0
#xrandr --output `xrandr -q | grep -w connected | awk 'NR==2 {print $1}'` --fb 3840x4320 --mode 1920x1080 --scale 2x2 --pos 0x0 --output eDP-1 --scale 1x1 --pos 0x2160
#exec --no-startup-id urxvt
# Pulse Audio controls
bindsym $mod+p exec /home/zj/.config/i3/rofi-power

bindsym XF86Search exec /home/zj/.config/i3/web-search.sh
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1%

bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1%

bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle # mute sound

# Sreen brightness controls
#bindsym XF86MonBrightnessUp exec inc_all
bindsym XF86MonBrightnessUp exec inc_all
bindsym XF86MonBrightnessDown exec dec_all
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec urxvt

# kill focused window
bindsym $mod+Shift+q kill

bindsym $mod+Shift+a exec /home/zj/.config/i3/rofi-audio
# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
bindsym $mod+d exec "rofi -modi drun,run -show run"
bindsym $mod+x exec "rofi -modi ssh -show ssh"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

for_window [class="^.*"] border pixel 2
set $workspaces1 "1:Terminal"
set $workspaces2 "2:Chrome"
set $workspaces3 "3:Code"
set $workspaces4 "4:Edit"
set $workspaces5 "5:Zotero"
set $workspaces6 "6:SSH"
set $workspaces7 "7:Steam"
set $workspaces8 "8:Game"
set $workspaces9 "9:Vbox"
set $workspaces10 "10:Others"

workspace $workspaces1 output $monitor1
workspace $workspaces2 output $monitor2
workspace $workspaces3 output $monitor1
workspace $workspaces4 output $monitor2
workspace $workspaces5 output $monitor2
workspace $workspaces6 output $monitor1
workspace $workspaces7 output $monitor1
workspace $workspaces8 output $monitor1
workspace $workspaces9 output $monitor2
workspace $workspaces10 output $monitor2

bindsym $mod+1 workspace $workspaces1
bindsym $mod+2 workspace $workspaces2
bindsym $mod+3 workspace $workspaces3
bindsym $mod+4 workspace $workspaces4
bindsym $mod+5 workspace $workspaces5
bindsym $mod+6 workspace $workspaces6
bindsym $mod+7 workspace $workspaces7
bindsym $mod+8 workspace $workspaces8
bindsym $mod+9 workspace $workspaces9
bindsym $mod+0 workspace $workspaces10

assign [class="Google-chrome"] $workspaces2

assign [class="jetbrains-pycharm-ce"] $workspaces3
assign [class="Code"] $workspaces3

assign [class="Typora"] $workspaces4
assign [class="XMind"] $workspaces4
assign [class="Xournal"] $workspaces4
assign [class="TeXstudio"] $workspaces4
assign [class="Gimp"] $workspaces4

assign [class="Zotero"] $workspaces5
assign [class="Evince"] $workspaces5

assign [title="ssh"] $workspaces6
assign [class="qpdfview"] $workspaces6

assign [class="steam"] $workspaces7
assign [class="Steam"] $workspaces7

assign [class="Binding\ of\ Isaac:\ Afterbirth+"] $workspaces8

assign [class="VirtualBox Machine"] $workspaces9

assign [class="shadowsocks-qt5"] $workspaces10
assign [class="Shutter"] $workspaces10
assign [class="xpad"] $workspaces10
assign [class="Gnome-calculator"] $workspaces10
# workspace layout 

for_window [class="shadowsocks-qt5"] floating enable resize set 1500 500
for_window [class="shadowsocks-qt5"] move position 300px 200px
for_window [class="Shutter"] floating enable
for_window [class="Shutter"] move position 300px 900px
for_window [title="Choose a location to save to"] move position center
for_window [title="Choose a location to save to"] floating enable
for_window [title="Choose a location to save to"] resize set 2000 1500
assign [title="Choose a location to save to"] $workspaces10
for_window [class="xpad"] move position 1400px 200px floating enable
for_window [class="Gnome-calculator"] move position 2400px 200px floating enable


for_window [workspace=$workspaces4] layout tabbed
for_window [workspace=$workspaces5] layout tabbed
for_window [workspace=$workspaces6] layout tabbed

for_window [class="Blueman-manager"] move position center
for_window [class="Blueman-manager"] floating enable
for_window [class="Blueman-manager"] resize set 2000 1500
for_window [class="Blueman-manager"] border pixel 3
# move focused container to workspace
bindsym $mod+Shift+m [class="netease-cloud-music"] scratchpad show; [class="netease-cloud-music"] move position center
for_window [class="netease-cloud-music"] floating enable
for_window [class="netease-cloud-music"] resize set 3000 2000
for_window [class="netease-cloud-music"] move scratchpad
for_window [class="netease-cloud-music"] border pixel 3

bindsym $mod+Shift+v [title="VirtualBox\ Manager"] scratchpad show; [title="VirtualBox\ Manager"] move position center
for_window [title="VirtualBox\ Manager"] floating enable
for_window [title="VirtualBox\ Manager"] resize set 3000 2000
for_window [title="VirtualBox\ Manager"] move scratchpad
for_window [title="VirtualBox\ Manager"] border pixel 3

bindsym $mod+Shift+w [class="wewechat"] scratchpad show; [class="wewechat"] move position center
for_window [class="wewechat"] floating enable
for_window [class="wewechat"] resize set 3000 2000
for_window [class="wewechat"] move scratchpad
for_window [class="wewechat"] border pixel 3

bindsym $mod+Shift+f [class="Nautilus"] scratchpad show; [class="Nautilus"] move position center
for_window [class="Nautilus"] floating enable
for_window [class="Nautilus"] resize set 3000 2000
for_window [class="Nautilus"] move scratchpad
for_window [class="Nautilus"] border pixel 3

bindsym $mod+Shift+t [instance="dropdown"] scratchpad show; [instance="dropdown"] move position center
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 3000 2000
for_window [instance="dropdown"] move scratchpad
for_window [instance="dropdown"] border pixel 3

bindsym $mod+Shift+z [class="Zeal"] scratchpad show; [class="Zeal"] move position center
for_window [class="Zeal"] floating enable
for_window [class="Zeal"] resize set 3000 2000
for_window [class="Zeal"] move scratchpad
for_window [class="Zeal"] border pixel 3

for_window [class="Goldendict" instance="goldendict"] focus
#for_window [class="Goldendict" instance="goldendict"] move position 1500px 300px sticky enable floating enable resize set 1500 1500
for_window [class="Goldendict" instance="goldendict"]  sticky enable floating enable resize set 2000 1500

bindsym $mod+Shift+1 move container to workspace $workspaces1
bindsym $mod+Shift+2 move container to workspace $workspaces2
bindsym $mod+Shift+3 move container to workspace $workspaces3
bindsym $mod+Shift+4 move container to workspace $workspaces4
bindsym $mod+Shift+5 move container to workspace $workspaces5
bindsym $mod+Shift+6 move container to workspace $workspaces6
bindsym $mod+Shift+7 move container to workspace $workspaces7
bindsym $mod+Shift+8 move container to workspace $workspaces8
bindsym $mod+Shift+9 move container to workspace $workspaces9
bindsym $mod+Shift+0 move container to workspace $workspaces10

# change focus
bindsym $mod+u move workspace to output right
bindsym $mod+Shift+u move workspace to output up
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 100
bindsym $mod+Shift+j move down 100
bindsym $mod+Shift+k move up 100
bindsym $mod+Shift+l move right 100

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left 50
bindsym $mod+Shift+Down move down 50
bindsym $mod+Shift+Up move up 50
bindsym $mod+Shift+Right move right 50

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+z focus child


# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)

mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 80 px
        bindsym j resize grow height 80 px
        bindsym k resize shrink height 80 px
        bindsym l resize grow width 80 px

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 80 px
        #bindsym Down resize grow height 80 px
        #bindsym Up resize shrink height 80 px
        #bindsym Right resize grow width 80 px

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+o workspace next
bindsym $mod+i workspace prev

bindsym $mod+Left workspace next
bindsym $mod+Right workspace next
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
        tray_output primary
        font pango:monospace 12, FontAwesome 12
        #output $monitor1
        #output $monitor2
        mode hide
        hidden_state hide
        modifier $mod
}
exec_always feh --bg-scale Pictures/wallpaper


bindsym button9 move left
bindsym button8 move right
#exec --no-startup-id dunst -config ~/.config/i3/dunstrc
